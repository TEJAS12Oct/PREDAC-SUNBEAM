#include<stdio.h> 

int main()
{
    // Arithmatic operator 
    // * , + , / 
    int num1, num2; 
    int ans; 
    printf("Enter the two numbers");
    scanf("%d%d",&num1,&num2); 
    ans = num1 + num2; 
    printf("%d",ans); 
    return 0; 
}