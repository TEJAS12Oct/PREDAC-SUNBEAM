#include<stdio.h> 

int main()
{
    // int a = 1;
    // int b = 2; 
    // int c = 3; 

    // a+=b+=c+=1; // += --> 3  
    // // c = c + 1 // 3 + 1 --> 4 
    // // b = b + c // 2 + 4 --> 6 
    // // a = a + b // 1 + 6 --> 7   

    // printf("a=%d b=%d c=%d",a,b,c); 

        int a,b,c; 

        a = b = c = -3; // = -> 3 times  
        printf("a=%d b=%d c=%d",a,b,c);

    return 0; 
}